* Test Suite

   * This module currently has no test suite, which is a problem.

   * in some cases, the output of a routine was spot-checked by myself, and then
     tested and validated via the phkdf and g3p-hash projects in this repo.

   * However, test coverage is still spotty, and I wouldn't be that suprised to find
     a bug or two in other routines not currently tested by these external packages.